export type QuestionnaireItem = {
    section: string,
    title: string,
    url: string,
    status: string
}

export type Params = {
    application_id: string,
    section_questions: string
}
  
  

 