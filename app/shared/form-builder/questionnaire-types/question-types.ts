export enum QuestionType {
    BOOLEAN = 'boolean',
    GRID = 'grid',
    TEXT = 'text',
    MULTI_SELECT = 'multi_select',
    DOCUMENT_UPLOAD = 'document_upload',
    READ_ONLY = 'read_only',
    SELECT = 'select',
    DATE = 'date',
    TEXT_AREA = 'text_area'
  }